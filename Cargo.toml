[package]
name = "sticker"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
dirs = "5"
eframe = "0.34"
egui_commonmark = "0.23"

[profile.release]
strip = true
lto = true
